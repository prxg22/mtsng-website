main
  section

    &#cover
      height: 100%
      align-items: center

      h1
        font-size: 48px
        animation: title-translate .4s 3s ease-in forwards


@keyframes title-translate
  100% {
    transform: translate(0, -20px);
  }
